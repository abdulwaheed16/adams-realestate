# Core Web Framework
Flask==3.1.2
Werkzeug==3.1.3

# Essential Libraries - using versions that are easier to install
requests==2.32.5
numpy==1.26.4  # Older, more stable version
opencv-python-headless==4.8.1.78 # A version known to have pre-built wheels
Pillow==10.1.0

# For YOLO, this can be tricky. If it fails, you may need to use a mock.
# Try this specific version first.
ultralytics==8.0.196

# LLM & API
openai==1.109.1

# Environment & Configuration
python-dotenv==1.1.1