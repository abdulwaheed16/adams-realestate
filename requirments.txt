# Core Web Framework
Flask==3.1.2
Werkzeug==3.1.3

# Essential Libraries - using versions with reliable pre-built wheels
requests==2.32.5
numpy==1.26.4
Pillow==10.1.0

# OpenCV - use a version known to have wheels and avoid GUI dependencies
opencv-python-headless==4.8.1.78

# Ultralytics - this can be tricky. We will handle it with a mock in the code if it fails.
ultralytics==8.0.196

# LLM & API
openai==1.109.1

# Environment & Configuration
python-dotenv==1.1.1